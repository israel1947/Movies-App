import { SlideParesPipe } from './slide-pares.pipe';

describe('SlideParesPipe', () => {
  it('create an instance', () => {
    const pipe = new SlideParesPipe();
    expect(pipe).toBeTruthy();
  });
});
